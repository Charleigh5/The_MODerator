import type { ModIntentSpec } from "./modIntentCompiler.js";
export interface BuildStep{order:number;action:string;verification:string;}
export interface BuildRecipe{plane:string;capabilityIds:string[];output:string;steps:BuildStep[];status:"EXECUTABLE_WITH_INPUTS"|"EXPERIMENT_PLAN";}
const stepsFor=(plane:string):BuildStep[]=>{if(/SAVE_FRTK|ROSTER/.test(plane))return[{order:1,action:"Hash and open source read-only; create working copy",verification:"source hash recorded and unchanged"},{order:2,action:"Resolve table/field by stable schema binding",verification:"binding authority proven for declared build"},{order:3,action:"Apply field-level changes to working copy",verification:"before/after diff contains only intended fields"},{order:4,action:"Write temporary/copy output and reopen",verification:"all intended fields survive reparse; invariants pass"},{order:5,action:"Expose copied save only",verification:"original hash matches baseline"}];if(/TEAM_BUILDER/.test(plane))return[{order:1,action:"Capture current Team Builder payload",verification:"baseline payload archived"},{order:2,action:"Resolve only proven hidden controls",verification:"control maps to source-backed binding"},{order:3,action:"Patch local payload and preserve unrelated keys",verification:"JSON structural diff reviewed"},{order:4,action:"Re-import/submit through Team Builder workflow",verification:"site accepts payload"},{order:5,action:"Verify in-game appearance",verification:"screenshot/runtime proof required"}];if(/FROSTBITE_FTC/.test(plane))return[{order:1,action:"Extract exact-build stock FTC/chunk",verification:"source chunk GUID/hash/build captured"},{order:2,action:"Decode table with exact schema",verification:"target table/row/field exists"},{order:3,action:"Apply minimal typed writes",verification:"field diff and decoded-size invariants pass"},{order:4,action:"Recompress/pad and package chunk-replacement .fbmod",verification:"independent .fbmod reread matches manifest/blob"},{order:5,action:"Load offline in MMC/FMT and test game",verification:"runtime proof; no unrelated regression"}];if(/RUNTIME/.test(plane))return[{order:1,action:"Require offline mode and exact recognized build",verification:"build/write gates pass"},{order:2,action:"Establish read-only observation baseline",verification:"target state/addresses reproducible"},{order:3,action:"Run one-variable guarded transaction/hook",verification:"compare-before-write + readback"},{order:4,action:"Capture behavior evidence",verification:"repeatable before/after result"},{order:5,action:"Promote binding only after repeatability",verification:"behavior-proven receipt"}];return[{order:1,action:"Resolve exact source asset/resource",verification:"asset identity and build captured"},{order:2,action:"Apply minimal isolated modification",verification:"binary/typed diff inspected"},{order:3,action:"Package separate .fbmod",verification:"package reread succeeds"},{order:4,action:"Offline runtime verification",verification:"visual/behavior evidence captured"}];};
export function buildRecipes(spec:ModIntentSpec):BuildRecipe[]{const by=new Map<string,Set<string>>();for(const e of spec.effects)for(const m of e.matches.slice(0,2)){const p=m.capability.surface;if(!by.has(p))by.set(p,new Set());by.get(p)!.add(m.capability.id);}return[...by].map(([plane,ids])=>({plane,capabilityIds:[...ids],output:/SAVE_FRTK|ROSTER/.test(plane)?"copied save/roster":/TEAM_BUILDER/.test(plane)?"patched Team Builder payload":/RUNTIME/.test(plane)?"offline experiment package":".fbmod / source-backed asset package",steps:stepsFor(plane),status:spec.readiness==="NEEDS_VERIFICATION"?"EXPERIMENT_PLAN":"EXECUTABLE_WITH_INPUTS"}));}
